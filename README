Dit is een gedistribueerde memory game.
Voor dit project werd er gebruik gemaakt van maven voor de verschillende projecten werkende te krijgen.
Projecten moeten dus geladen worden als maven project in eclipse/intellij.
Er wordt voor de communicatie gebruik gemaakt van RMI. 

De algemene structuur is het volgende:
## MEMORYSTART ##
De module start op en met de verschillende final ints kan je kiezen hoeveel db (heeft als naar db1.db in de map van intellij project) en servers die in het begin opstart. (Nu is het 1 db en 1 server)
De client doet een RMI op de dispatcher die deel is van de StartServer en krijgt dan te horen welke server die moet contacteren om naar in een lobby te komen.

## MEMORYSERVER ##
Heeft een basis implementatie met een beetje code maar dit is nog niet uitgetest.

## MEMORYDATABASE ##
Code voor het ontvragen van de database. Bevat onderandere de code voor het maken van een database en een tabel met user (enkel naam en ww op dit moment)

## MEMORYCLIENT ##
Start op met een login scherm. Bij duwen op login -> rmi -> opvragen van server aan dispatcher kant. 
Vanaf dan zou die moeten communiceren met de server via rmi.
	